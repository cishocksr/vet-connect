<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="004-create-saved-resources-table" author="developer">
        <createTable tableName="saved_resources">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_saved_resources_user"
                             references="users(id)"
                             deleteCascade="true"/>
            </column>
            <column name="resource_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_saved_resources_resource"
                             references="resources(id)"
                             deleteCascade="true"/>
            </column>
            <column name="notes" type="TEXT"/>
            <column name="saved_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint
                constraintName="uk_user_resource"
                tableName="saved_resources"
                columnNames="user_id, resource_id"/>

        <createIndex indexName="idx_saved_resources_user" tableName="saved_resources">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_saved_resources_resource" tableName="saved_resources">
            <column name="resource_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>