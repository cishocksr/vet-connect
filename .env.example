# ==========================================
# VETCONNECT ENVIRONMENT VARIABLES
# ==========================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values
# 3. NEVER commit .env to version control
# 4. Minimum 512-bit JWT secret: openssl rand -base64 64
#
# ==========================================

# ===== DATABASE =====
POSTGRES_DB=vet_connect
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_123!

POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ===== JWT AUTHENTICATION =====
# CRITICAL: Use openssl rand -base64 64 to generate
JWT_SECRET=REPLACE_WITH_MINIMUM_64_CHARACTER_SECRET_FROM_OPENSSL_COMMAND

JWT_EXPIRATION=86400000         # 24 hours in milliseconds
JWT_REFRESH_EXPIRATION=604800000 # 7 days in milliseconds
JWT_ISSUER_URI=http://localhost:8080

# ===== REDIS (Token Blacklist) =====
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=                   # Optional: Set for production

# ===== CORS =====
# Development
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Production (replace with actual domains)
# CORS_ALLOWED_ORIGINS=https://vetconnect.com,https://www.vetconnect.com

# ===== FILE UPLOADS =====
FILE_UPLOAD_DIR=uploads/profile-pictures

# ===== PGADMIN (Optional - for database management) =====
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin

# ===== FRONTEND =====
VITE_API_BASE_URL=http://localhost:8080/api